import jdk.jfr.Category
import java.time.temporal.TemporalAmount

fun main(){
   var x= currentAccount("2345667", "Grace", 3000)
   println(x.balance)
fruit("banana")
}
data class currentAccount(var accountNumber:String, var accName:String, var balance:Int)
  fun deposit(amount:Double):String{
     var sum=amount
      //var increament= amount+ deposit
     println(sum)
 }
 fun withdraw(amount: Double){

 }
fun fruit(sweet:String):String{
    var tunda="banana"
    sweet.forEach { lol->
    println(lol.toString()+[0]+sweet[2]+sweet[4])
        println(tunda)
    }


    return ("tunda")
}
//data class big(var name:String, var weight:Double, var price:Int, var category: String)
//    fun product(name:String):Boolean{
//        if (big is big)





